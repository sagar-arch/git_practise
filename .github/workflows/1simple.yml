#To check first github actions command on push

name: shell-command
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo-a-string
        run: echo "helloworld"
      - name: multiline
        run: |
          node -v
          npm -v
